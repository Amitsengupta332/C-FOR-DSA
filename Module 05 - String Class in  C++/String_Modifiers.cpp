#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s = "Hello World";
    // string s1 = "HI";
    // s+= s1;
    // s.append(s1); // Append s1 to s
    // s.push_back('A');
    // s+= " C++"; // Concatenate " C++" to s
    // s.pop_back();

    //     s.assign() -> assign string.
    // s.erase() -> erase characters from the string.
    // s.replace() -> replace a portion of the string.
    // s.insert() -> insert a portion to a specific position.

    s = "GEllO";
    cout << s << endl; // Output the concatenated string
    return 0;
}